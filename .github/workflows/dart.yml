name: Dart

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: panda
    env:
      ANDROID_PLATFORM_TOOLS_PATH: /home/hannes/devel/android-sdk/platform-tools
      
    steps:
      - uses: actions/checkout@v2

      - name: Setup flutter SDK
        uses: subosito/flutter-action@v2
        with:
          cache: true
             
      - name: Setup melos
        run: dart pub global activate melos

      - run: flutter doctor -v
      
      - run: flutter precache

      - name: melos bootstrap
        run: dart pub global run melos bootstrap
        
      - name: melos checkformat
        run: dart pub global run melos run checkformat
        
      - name: melos analyze
        run: dart pub global run melos run analyze
        
      - name: melos test
        run: dart pub global run melos test
        
      - name: make ODROID C4 gpio chips accessible
        run: |
          $ANDROID_PLATFORM_TOOLS_PATH/adb shell chmod 0777 /dev/gpiochip0
          $ANDROID_PLATFORM_TOOLS_PATH/adb shell chmod 0777 /dev/gpiochip1
        
      - name: run flutter_gpiod_test_app on ODROID C4
        working-directory: packages/flutter_gpiod_test_app
        run: flutter test -d "ODROID C4" -t odroidc4 integration_test/gpio_test.dart
